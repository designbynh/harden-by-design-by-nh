# Copy to local-db.env (gitignored) if you want scripts/mysql-option-row.sh
# Do not commit real credentials.

DB_HOST=127.0.0.1
DB_NAME=your_database
DB_USER=your_user
DB_PASSWORD=your_password
TABLE_PREFIX=wp_
